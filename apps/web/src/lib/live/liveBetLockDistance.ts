/**
 * Straight-line distance (meters) from the vehicle to the active turn pin at
 * which betting must close. Kept in sync with tick route + placeLiveBet.
 */
export const LIVE_BET_LOCK_DISTANCE_M = 50;
